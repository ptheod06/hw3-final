syntax = "proto3";

package geo;

// TODO: Implement me

service Geo {
  rpc Nearby(Request) returns (Result);
}

message Request {
  float lat = 1;
  float lon = 2;
}

message Result {
  repeated string hotelIds = 1;
}
